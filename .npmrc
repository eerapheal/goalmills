# GoalMills PNPM Configuration
# Ensure devDependencies (like turbo, typescript, build tools) are installed even when NODE_ENV=production
production=false
auto-install-peers=true
dedupe-peer-dependents=true
engine-strict=false
